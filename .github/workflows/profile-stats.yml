name: Update profile stats

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "profile_stats.py"
      - ".github/workflows/profile-stats.yml"
  schedule:
    - cron: "17 8 * * *"

permissions:
  contents: write

concurrency:
  group: profile-stats
  cancel-in-progress: true

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Generate profile panels
        env:
          GITHUB_TOKEN: ${{ secrets.PROFILE_STATS_TOKEN }}
          USER_NAME: mjr267
        run: python profile_stats.py

      - name: Commit generated SVGs
        run: |
          FILES="dark_mode.svg light_mode.svg about_dark.svg about_light.svg stack_dark.svg stack_light.svg"
          if git diff --quiet -- $FILES; then
            echo "Profile panels are already current."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add $FILES
          git commit -m "chore: refresh profile panels"
          git push
